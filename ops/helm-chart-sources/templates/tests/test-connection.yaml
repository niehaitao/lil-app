apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lil-app.fullname" . }}-test-connection"
  labels:
    {{- include "lil-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lil-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
